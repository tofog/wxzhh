
patch:
  schema_list:
    - {schema: tiger}    #虎码2-4任意码整句
    - {schema: wanxiang} #拼音方案
    - {schema: mets}    #虎码23整句
    - {schema: xumn}    #虎码原码整句
    - {schema: 9jianhu} #9键虎
    
  key_binder/bindings/+:
    - { when: always, select: .next, accept: Control+Shift+1 }                  # 在最近的两个方案之间切换
    - { when: always, select: .next, accept: Control+Shift+exclam }             # 在最近的两个方案之间切换